// This ensures that SvelteKit loads all pages properly
export const prerender = true;
export const ssr = true;
export const csr = true;

// Use this instead if you want client-side rendering only
// export const ssr = false;
// export const csr = true;
